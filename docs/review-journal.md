# Review Journal

This journal records the domain cases that matter before widening the public API.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its blockchain tooling focus without claiming live deployment or external usage.

## Cases

- `baseline`: `event finality`, score 169, lane `ship`
- `stress`: `nonce pressure`, score 165, lane `ship`
- `edge`: `settlement risk`, score 214, lane `ship`
- `recovery`: `proof depth`, score 206, lane `ship`
- `stale`: `event finality`, score 200, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
